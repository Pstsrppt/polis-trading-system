[project]
name = "polis-gateway"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn[standard]", "websockets", "asyncpg", "httpx>=0.27", "redis[asyncio]>=5.0", "polis-event-bus", "qrcode[pil]>=7.4", "Pillow>=9.5.0"]
[tool.uv.sources]
polis-event-bus = { workspace = true }
