# Command Center (dashboard)
The live "Google Maps of the company": org chart, agent registry, AI economy,
event bus feed, policy status. A static prototype ships as `polis-command-center.html`
at the repo root for instant preview; production build is Next.js + WebSocket here.

`app/` routes · `components/` UI · `widgets/` org-chart · registry · economy · feed
